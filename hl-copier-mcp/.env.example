# Адрес REST API бота hl-copier (по умолчанию локальный)
HLC_URL=http://127.0.0.1:8787

# Пароль UI бота — нужен ТОЛЬКО если в боте включена авторизация (auth_enabled).
# Если авторизация выключена — оставь пустым.
HLC_PASSWORD=
