# Скопируй в .env и впиши свои ключи. .env в .gitignore — в git не попадёт.
# Для testnet ключи берутся на https://testnet.binancefuture.com (нужны права Futures).
BINANCE_API_KEY=
BINANCE_API_SECRET=
